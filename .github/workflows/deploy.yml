name: Deploy v1 (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow notice
        run: echo "This workflow is intentionally disabled. Use Deploy v2 instead."
