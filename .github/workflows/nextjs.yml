name: Legacy Next.js Sample (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled legacy sample workflow. Use Deploy v2 (GitHub Pages)."
